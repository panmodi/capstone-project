# capstone-project
Use OpenML(ID: 43438) which has books data derived from GoodReads site. We can use this to predict if a book can be successful from the metadata of books like author, publisher, genre and series info
